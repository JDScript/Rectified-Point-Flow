_target_: rectified_point_flow.data.PointCloudDataModule
num_points_to_sample: 5000
min_parts: 2
max_parts: 100
min_points_per_part: 20
multi_anchor: false

data_root: ${data_root}
dataset_names: ["objaverse"]

batch_size: 8
num_workers: 8
limit_val_samples: 0  # 0 for all samples